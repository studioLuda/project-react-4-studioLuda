import { createSlice } from '@reduxjs/toolkit';

const initialState = {

};

const reducers = {};

const { actions, reducer } = createSlice({
  name: 'application',
  initialState,
  reducers,
});

export default reducer;
